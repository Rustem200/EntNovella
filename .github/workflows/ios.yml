name: iOS

# Запуск вручную: вкладка Actions -> iOS -> Run workflow.
# Нужны секреты UNITY_USERNAME (почта аккаунта Unity) и UNITY_PASSWORD.
on:
  workflow_dispatch:

jobs:
  build:
    name: Собрать .ipa
    runs-on: macos-15
    timeout-minutes: 120

    steps:
      - name: Забрать исходники
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Проверить, что секреты доходят
        env:
          U: ${{ secrets.UNITY_USERNAME || secrets.UNITY_EMAIL }}
          P: ${{ secrets.UNITY_PASSWORD }}
        run: |
          echo "длина логина: ${#U}, длина пароля: ${#P}"
          if [ -z "$U" ] || [ -z "$P" ]; then
            echo "::error::Секреты не пришли. Проверь, что они заведены в Repository secrets (не Environment) и называются UNITY_USERNAME и UNITY_PASSWORD."
            exit 1
          fi

      - name: Поставить Unity с модулем iOS
        uses: RageAgainstThePixel/unity-setup@v2
        with:
          unity-version: 6000.3.12f1
          build-targets: iOS
          cache-installation: true

      - name: Активировать лицензию Personal
        uses: RageAgainstThePixel/activate-unity-license@v2
        with:
          license: Personal
          username: ${{ secrets.UNITY_USERNAME || secrets.UNITY_EMAIL }}
          password: ${{ secrets.UNITY_PASSWORD }}

      - name: Unity -> Xcode-проект
        run: |
          "$UNITY_EDITOR_PATH" \
            -quit -batchmode -nographics \
            -projectPath "$GITHUB_WORKSPACE" \
            -buildTarget iOS \
            -executeMethod CIBuild.BuildIOS \
            -logFile -

      - name: Xcode -> архив без подписи
        run: |
          cd build/iOS
          xcodebuild \
            -scheme Unity-iPhone \
            -configuration Release \
            -sdk iphoneos \
            -archivePath "$PWD/app.xcarchive" \
            archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES

      - name: Упаковать .ipa
        run: |
          cd build/iOS
          mkdir -p Payload
          cp -R app.xcarchive/Products/Applications/*.app Payload/
          zip -qry EntNovella.ipa Payload

      - name: Выложить артефакт
        uses: actions/upload-artifact@v4
        with:
          name: EntNovella-ipa
          path: build/iOS/EntNovella.ipa
          retention-days: 30
